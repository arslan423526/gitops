---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: changelog
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: changelog
  policy:
    semver:
      range: '>=1.0.0'
